export default class Submission {
	constructor(id, group, points, submitterList) {
	  this.id = id; // Define the entity with an id property
	  this.group = group;
	  this.points = points;
	  this.submitterList = submitterList;
	}
}